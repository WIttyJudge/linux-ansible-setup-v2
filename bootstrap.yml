---
- name: Bootstrap a fresh Arch Linux install
  hosts: local

  vars_files:
    - custom.yml
    - secret.yml

  roles:
    - role: system
      tags:
        - system

    - role: dotfiles
      tags:
        - dotfiles

    - role: wallpapers
      tags:
        - wallpapers
